"""Project-local analysis and figure-generation scripts.

This file exists so ``import scripts.<module>`` resolves here when the project
directory is on ``sys.path``, instead of the repository-level ``scripts/``
package (which would shadow a namespace-only ``scripts/`` directory).
"""
